services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: leadlog
      POSTGRES_PASSWORD: leadlog
      POSTGRES_DB: leadlog
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
